name: Dataset Action
description: Dataset deployment
inputs:
  folder:
    description: 'Working-dir folder'
    required: true
    default: 'personal'
  type:
    description: ''
    required: true
    default: ''
  name:
    description: ''
    required: true
    default: ''
runs:
  using: "composite"
  steps:
    - name: Dataset deployment
      env:
        name: ${{ inputs.name }}
        API: ${{ github.action_path }}/.payload_templates/api
        type: ${{ inputs.type }} | tr [:upper:] [:lower:]
      run: |
        cd ${{ inputs.folder }}
        babylon api dataset create $name -i "$API/dataset.$type.yaml" -t $type -s
      shell: bash
